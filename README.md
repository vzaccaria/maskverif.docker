# Build the MaskVerif container and run the tests

First you must build the container:

```
make build-container
```

Then you can run the tests of the costructions presented in the IET paper as
follows:

```
make iet-run
```
